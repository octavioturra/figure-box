Polymer('figure-box', {
	/**
	 * The `url` of image.
	 *
	 * @property src
	 * @type string
	 */
	src: '',
	/**
	 * The `caption` is the ALT and the main title of the box, at the same time.
	 *
	 * @property caption
	 * @type string
	 */
	caption: null,
	/**
	 * The `href` is the location that the box action will redirect you.
	 *
	 * @property href
	 * @type string
	 */
	href: null
});;
Polymer('figure-box');